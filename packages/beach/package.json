{
  "private": true,
  "name": "beach",
  "description": "beach is sandbox.",
  "version": "1.0.1",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "eslint-plugin-summer": "^1.0.1"
  },
  "scripts": {
    "prepare": "exit 0",
    "test": "echo \"a === undefined\" | eslint --stdin --fix-dry-run --format=json"
  }
}
