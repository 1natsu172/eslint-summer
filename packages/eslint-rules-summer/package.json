{
  "name": "eslint-rules-summer",
  "version": "1.0.1",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "prepare": "yarn clean && yarn build",
    "clean": "rimraf ./dist",
    "build": "tsc -p . --outDir dist"
  }
}
