{
  "name": "eslint-plugin-summer",
  "version": "1.0.0",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "prepare": "yarn clean && yarn build",
    "clean": "rimraf ./dist",
    "build": "tsc -p . --outDir dist"
  },
  "dependencies": {
    "eslint-rules-summer": "^1.0.0"
  },
  "devDependencies": {},
  "peerDependencies": {
    "eslint": "^4.0.0 || ^5.0.0 || ^6.0.0"
  }
}
